include ../messagesAvatar/messagesAvatar.pug

mixin user(name, message, time, count, avatarURL) 
    article.user
        +messagesAvatar(avatarURL)
        .user__content
            .user__header
                h4=name 
                p=time  
            .user__main
                p=message
                p=count

