mixin chatPageInput(type, placeholder)
        .input-wrapper
            input(
                class="chat-page-input"
                type=type
                placeholder=placeholder
                )
